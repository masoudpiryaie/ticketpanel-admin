// import React, { createContext, useContext } from "react";

// const WPContext = createContext({
//   ajaxUrl: WPData.ajaxUrl,
//   nonce: WPData.nonce,
// });

// export const useWPData = () => useContext(WPContext);

// export const WPProvider = ({ children }) => (
//   <WPContext.Provider value={{ ajaxUrl: WPData.ajaxUrl, nonce: WPData.nonce }}>
//     {children}
//   </WPContext.Provider>
// );
